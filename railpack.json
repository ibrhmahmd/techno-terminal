{
  "_cache_bust": "v4",
  "build": {
    "cmd": "pip install -e ."
  },
  "start": {
    "cmd": "gunicorn app.api.main:app -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000 --workers 2 --timeout 300 --graceful-timeout 60 --pid /tmp/gunicorn.pid --worker-tmp-dir /tmp"
  }
}
