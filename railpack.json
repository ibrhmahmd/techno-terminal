{
  "_cache_bust": "v3",
  "build": {
    "cmd": "pip install -e ."
  },
  "start": {
    "cmd": "gunicorn app.api.main:app -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000 --workers 2 --timeout 120 --graceful-timeout 30 --pid /tmp/gunicorn.pid"
  }
}
